const admin = require('firebase-admin');

// We need a script to read products from Firestore.
// Or we can just use the DataContext directly if we have an RPC action? Wait, we can just use rpc_action tool.
